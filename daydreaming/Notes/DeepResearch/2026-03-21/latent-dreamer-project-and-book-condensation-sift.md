# Sift: Latent Dreamer Project And Book Condensation Chat

Source chat:
`chats/2026-03-21_22-10-37_Claude_Chat_Latent_dreamer_project_and_book_condensation.md`

Purpose:
Keep the durable framing from that discussion without letting stale status
claims or speculative research threads steer active work.

## Keep

1. Chain A vs Chain B is still the right split.
   - Chain A: deliberate planning / analogy / evaluation.
   - Chain B: reminding / serendipity / accidental bridge discovery.
   - The rule connection graph is the shared substrate under both.

2. "Prosthetic inner life" is good external framing.
   - The kernel is not a replacement for the LLM.
   - It gives the LLM capabilities it does not natively have:
     persistent structure, typed episodic residue, explicit retrieval bias,
     involuntary goal activation, and graphable rule structure.

3. The fixture question is real.
   - Puppet Knows was sufficient to prove writeback divergence.
   - It may not be rich enough to exercise true graph search / serendipity.
   - This should stay an explicit question as Chain B work advances.

4. The rule graph only matters if it changes reachable behavior.
   - Nodes and edges are not enough.
   - The relevant test is whether structural paths become inspectable,
     traversable, and later behaviorally load-bearing.

## Correct / Demote

1. Status claims in the chat are stale.
   - Since that discussion, the kernel moved beyond initial RuleV1 landing.
   - The family layer now has generic family-plan dispatch, rule provenance in
     traces and residue episodes, and graph query helpers.

2. GradMem / RLM / DSPy are backlog material, not active direction.
   - They are useful analogies around write quality, memory navigation, and
     prompt/interface optimization.
   - They should not displace the current deep-build sequence.

3. The chat is interpretive, not authoritative.
   - Use it for framing.
   - Use `current-sprint.md`, the condensation docs, and actual kernel commits
     for project-state claims.

## Main Path

Project lane:
- RuntimeThoughtBeatV1 already passed the writeback divergence gate.
- The open project question is policy and presentation, not whether feedback
  matters at all.

Deep-build lane:
- Control is now structurally owned through family activation and planning.
- Provenance now records which rules and edges shaped a cycle.
- Graph inspection now exists through bounded queries and path explanation.

Next honest deep-build questions:
1. Can rule/edge provenance become intervention-sensitive?
   - Remove a rule or edge and show reachable paths or residue retrieval change.
2. Is Puppet Knows rich enough to produce non-trivial graph paths?
3. What is the first real Chain B slice after graph inspection?
   - Not full serendipity.
   - Likely a bounded bridge/path probe over a richer rule set.
